# spring.application.name=spring
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=software

server:
  port: 8080

spring:
  application:
    name: spring
  data:
    mongodb:
      host: localhost
      port: 27017
      database: software
